// 공통 컴포넌트
export { default as LoadingSpinner } from "./components/LoadingSpinner";

// 필요한 경우 다른 공통 요소들도 여기서 export 가능

// 스타일 경로 (CSS 모듈이 아니므로 import 후 export는 필요없음)
// 참고로 CSS 파일은 직접 컴포넌트에서 import '../../../shared/styles/common.css' 형태로 사용합니다.
